import { PrismLotus } from "@/components/prism-lotus";

export default function Loading() {
  return (
    <div className="flex items-center justify-center min-h-[60vh]">
      <PrismLotus size="lg" variant="icon" colorScheme="default" />
    </div>
  );
}
