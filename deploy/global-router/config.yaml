# Global Router Configuration
router:
  domain: featuresignals.com
  email: admin@featuresignals.com # For Let's Encrypt notifications

  # TLS
  tls:
    cache_dir: /data/certs
    port: 443

  # DNS (for future multi-region geolocation)
  dns:
    enabled: false
    port: 53

  # Domains this router handles
  domains:
    - name: featuresignals.com
      type: static
      root: /data/www/website

    - name: docs.featuresignals.com
      type: static
      root: /data/www/docs

    - name: api.featuresignals.com
      type: proxy
      target: http://featuresignals-server:8080
      rate_limit: 1000/min
      # rate_limit_algo: leaky  # uncomment for leaky bucket instead of sliding window
      # circuit_breaker:
      #   failure_threshold: 5
      #   cooldown: 30s
      #   half_open_max_requests: 1

    - name: app.featuresignals.com
      type: proxy
      target: http://featuresignals-dashboard:3000
      rate_limit: 100/min

    - name: signoz.featuresignals.com
      type: proxy
      target: http://signoz-frontend:3301
      rate_limit: 20/min
      auth: ops
      circuit_breaker:
        failure_threshold: 3
        cooldown: 60s

  # Rate limiting
  rate_limit:
    default: 100/min
    burst: 50
    window: 60s
    # algo: leaky  # "sliding" (default) or "leaky" for smoother burst handling

  # Connection limiting & IP tracking bounds
  conn_limit: 100
  max_ips: 100000

  # Clusters
  cluster:
    name: us-001
    region: us
    public_ip: ""

  peer_clusters: []
