/**
 * Console Page — No-op shell.
 *
 * The Console layout (TopBar + ThreeZone + BottomBar + overlays)
 * is now rendered by the parent (app)/layout.tsx as the permanent
 * shell for all authenticated pages.
 *
 * This page returns null — the layout handles everything.
 */

export default function ConsolePage() {
  return null;
}
