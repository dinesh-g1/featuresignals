# FeatureSignals Server — Local Development Environment
# Copy this file:  cp .env.example .env
# Then adjust values as needed.

# ─── Database ────────────────────────────────────────────────────────────────
# Matches docker-compose.yml defaults. Change only if you use a custom setup.
# Local dev uses sslmode=disable; in production use sslmode=require (or your provider's TLS settings).
DATABASE_URL=postgres://fs:fsdev@localhost:5432/featuresignals?sslmode=disable

# ─── Server ──────────────────────────────────────────────────────────────────
PORT=8080

# ─── Authentication ──────────────────────────────────────────────────────────
# CHANGE THIS in production. Any string works for local dev.
JWT_SECRET=dev-secret-change-in-production
TOKEN_TTL_MINUTES=60
REFRESH_TTL_HOURS=168

# ─── Logging ─────────────────────────────────────────────────────────────────
# Options: debug | info | warn | error
LOG_LEVEL=debug

# ─── CORS ────────────────────────────────────────────────────────────────────
CORS_ORIGIN=http://localhost:3000

# ─── MSG91 (SMS & Email OTP) ─────────────────────────────────────────────────
# Sign up at https://msg91.com to get your auth key and template IDs.
# MSG91_AUTH_KEY=your-msg91-auth-key
# MSG91_TEMPLATE_ID=your-sms-template-id        # SMS OTP template (phone verification)
# MSG91_SENDER_ID=FEATSIG

# MSG91 Email OTP (used for verify-first signup flow)
# MSG91_EMAIL_TEMPLATE_ID=your-email-template-id  # MSG91 email template with {{otp}} variable
# MSG91_EMAIL_DOMAIN=mail.featuresignals.com
# MSG91_EMAIL_FROM=noreply@featuresignals.com
# MSG91_EMAIL_FROM_NAME=Feature Signals

# ─── URLs ────────────────────────────────────────────────────────────────────
# APP_BASE_URL=http://localhost:8080
# DASHBOARD_URL=http://localhost:3000

# ─── PayU Billing ────────────────────────────────────────────────────────────
# PAYU_MERCHANT_KEY=your-payu-key
# PAYU_SALT=your-payu-salt
# PAYU_MODE=test
